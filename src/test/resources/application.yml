spring:
   datasource:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:db;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
   jpa:
      show-sql: true
logging:
   file: target/test-classes/logs/poc-fwk-logger.log
   level:
      poc.fwk: debug